Deploy-ready frontend patch.

What changed:
- The app now remembers the backend API URL in localStorage.
- Open the Settings tab, paste your deployed FastAPI base URL (for example, https://your-backend.onrender.com), click Save API URL, then Test backend.
- No code edits are needed after deployment.

Files:
- index.html
